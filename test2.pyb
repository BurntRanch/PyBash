$ Include the sha256 function from the hashlib library
INCLUDE sha256 FROM hashlib;
$ INCLUDE sys;

$ Define the print function
DEFINE FUNC hash ARGS pswd;

    $ Hash input
    CALL sha256 ARGS (pswd);

    $ hex digest it
    CALL RETURN.hexdigest;

    SET h TO RETURN;

    RETURN h;

$ End of function definition
EXIT FUNC;
