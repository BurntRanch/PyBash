INCLUDE test2;

$ this is a speed test, it will hash numbers from 1 to 5k.
WHILE True;

    CALL test2.hash_pswd ARGS (str(input("insert value to hash: ")).encode());
    
    CALL print ARGS (RETURN);

    BREAK;

ENDWHILE;
