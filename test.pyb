INCLUDE sha256 FROM hashlib;

$ SET result TO RETURN;

SET counter TO 0;

$ this is a speed test, it will hash numbers from 1 to 5k.
FOR list(range(1, 5000)) AS i;

    CALL sha256 ARGS (str(i).encode());
    
ENDFOR;
